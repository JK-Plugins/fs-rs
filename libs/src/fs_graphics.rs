enum ScanLine {
    HorAndVer = 1,
    Hor,
    Ver,
}

enum TargetChannel {
    RgbAndAlpha = 1,
    Rgb,
    Alpha,
}

// pub fn minimax_8(value: u8, mode: ScanLine, target: TargetChannel) -> u8 {
// }
